package controlador;

import java.io.IOException;
import java.sql.SQLException;
import java.util.ArrayList;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import modelo.*;

@WebServlet("/escribeFichero")
public class escribeFichero extends HttpServlet {
	private static final long serialVersionUID = 1L;

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		DAO d = new DAO("encuesta");
		try {
			ArrayList<Palabras> p = (ArrayList<Palabras>) d.buscar();
			ArrayList<String> palabraVoto = new ArrayList<String>();
			String[] palabra;
			for (Palabras pals : p) {
				palabraVoto.add(palabra);
			}
			d.escribeFicheroArraylist(palabraVoto, true);
			
		} catch (SQLException e) {
			System.out.println(e.getMessage());
		}
	}


}
